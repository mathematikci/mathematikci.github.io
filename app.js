const form=document.querySelector('#pairForm');const code=document.querySelector('#pairCode');const status=document.querySelector('#status');const fullscreen=document.querySelector('#fullscreenBtn');const stage=document.querySelector('.stage');code.addEventListener('input',()=>{const digits=code.value.replace(/\D/g,'').slice(0,6);code.value=digits.length>3?`${digits.slice(0,3)} ${digits.slice(3)}`:digits});form.addEventListener('submit',event=>{event.preventDefault();const digits=code.value.replace(/\D/g,'');if(digits.length!==6){status.textContent='Lütfen 6 haneli eşleştirme kodunu girin.';code.focus();return}status.textContent='Yardımcı uygulama ve sinyal sunucusu bağlandığında eşleştirme burada başlayacak.'});fullscreen.addEventListener('click',async()=>{try{if(!document.fullscreenElement)await stage.requestFullscreen();else await document.exitFullscreen()}catch{status.textContent='Tam ekran modu bu tarayıcıda kullanılamıyor.'}});
